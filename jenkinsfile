pipeline{
    agent any
    tools {nodejs'node'}
    stages {
        stage('test'){
            steps{
                sh'npm install'
                sh'npm test'
                
            }
        }
        stage('bild'){
            steps{
                sh'docker-compose build'
                sh'docker-compose up'
            }
        }
    }
}