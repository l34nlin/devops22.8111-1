pipeline{
    agent any
    stages {
        stage('build TADS'){
            steps{
                sh''' 
                docker-compose build
                docker-compose up
                npm test
                '''
                
            }
        }
    }
}